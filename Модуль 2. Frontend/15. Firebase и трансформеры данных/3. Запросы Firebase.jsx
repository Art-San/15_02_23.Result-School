// Документация Firebase (REST API): ссылка
// https://firebase.google.com/docs/reference/rest/database

// Обрати внимание!
// На 00:27 в документации фраза “HTTP is required” 
// после автоматического перевода перевелась как “HTTP не требуется”. 
// Это, конечно же, не так, поэтому при использовании переводчика будьте внимательны.