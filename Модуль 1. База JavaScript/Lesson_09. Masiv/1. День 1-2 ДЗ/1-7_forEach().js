const methods = [
    'push()',
    'pop()',
    'shift()',
    'unshift()',
    'slice()',
    'splice()',
    'forEach()',
    'map()',
    'find()',
    'findIndex()'
  ];
  
  methods.forEach(name => console.log(`Метод ${name}`))

//   https://codepen.io/asava1980/pen/poLZbzv
  
  // Вывод 
  // Метод push()
  // Метод pop()
  // Метод shift()
  // Метод unshift()
  // Метод slice()
  // Метод splice()
  // Метод forEach()
  // Метод map()
  // Метод find()
  // Метод findIndex()


//   names.forEach((name, index, array) => {
//         console.log('name', name)
//         console.log('name', index)
//         console.log('name', array)
//     });